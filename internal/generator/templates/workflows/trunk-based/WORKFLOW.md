# <<.ProjectName>> — Trunk-Based Development

> **Generated by flowkit.** Replace placeholders as needed.

---

## Branching Strategy

```
<<.MainBranch>>                single long-lived branch
  └─ <short-lived-branch>      max 1 day, no PR needed for small teams
```

### Ground Rules

1. **Only `<<.MainBranch>>` exists long-term.** No `develop`, no long-lived feature branches.
2. **Branches live < 1 day.** Split work into tiny increments.
3. **Merge fast, merge often.** Several merges per day.
4. **Pair/mob programming encouraged.** Reduces need for PR reviews.
5. **Feature flags** for incomplete work — never branch for hiding code.

---

## Commit Message Convention

Use **Conventional Commits**:

```
<type>(<scope>): <description>
```

**Rules:** English, imperative mood, max 72 chars.

Examples:
```
feat(auth): add OAuth2 login flow
fix(api): validate request body before processing
```

---

## Task Workflow

### Start
```bash
git checkout <<.MainBranch>> && git pull
git checkout -b <short-name>
```

### During Development (same day)
```bash
git add <file>
git commit -m "<type>(<scope>): <message>"
git fetch origin
git rebase origin/<<.MainBranch>>
```

### Merge Back (same day)
```bash
git checkout <<.MainBranch>> && git pull
git merge <branch-name>
git push origin <<.MainBranch>>
git branch -d <branch-name>
```

---

## Code Quality Checklist

- [ ] `<<.LintCmd>>` — no errors
- [ ] `<<.BuildCmd>>` — no errors
- [ ] Feature flag untuk perubahan incomplate
- [ ] No commented-out code
- [ ] Proper error handling

---

*Generated by flowkit. Adapt to your project's needs.*
